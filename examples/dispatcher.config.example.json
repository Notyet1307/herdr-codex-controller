{
  "version": 1,
  "parentIssue": 100,
  "readyLabel": "ready-for-agent",
  "releaseAcceptanceCriteria": [
    "The selected Issue acceptance criteria and repository validation commands pass.",
    "A fresh aggregate reviewer reports no critical or major finding at the exact candidate SHA.",
    "The exact candidate passes required pull-request checks and is squash-merged with a matching HEAD guard."
  ],
  "reviewFocus": [
    "The selected Issue specification and repository standards.",
    "Security, authorization, data integrity, migration safety, and regression coverage."
  ],
  "postMerge": {
    "requiredWorkflows": [
      "Test Backend",
      "Playwright Tests",
      "Test Docker Compose"
    ],
    "pollIntervalMs": 15000,
    "timeoutMs": 7200000
  }
}
