{
  "name": "herdr-codex-controller",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A thin Codex-first release delivery controller for ordered GitHub issues.",
  "engines": {
    "node": ">=22.16.0"
  },
  "bin": {
    "herdr-codex": "./dist/src/cli.js",
    "herdr-codex-goal": "./dist/src/goal-cli.js"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "rm -rf dist && tsc -p tsconfig.json && chmod +x dist/src/cli.js dist/src/goal-cli.js",
    "test": "npm run build && node --test --test-concurrency=1 dist/test/*.test.js",
    "verify": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@openai/codex": "0.152.0",
    "ajv": "8.20.0",
    "typescript": "5.8.3"
  },
  "license": "MIT"
}
